rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DEMO: workspace i ndare
    match /spaces/{wid}/meta/{doc} {
      allow read, write: if true;
    }
    match /spaces/{wid}/tasks/{tid} {
      allow read, write: if true;
    }
  }
}
